{
  "name": "okx-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "agent": "bun run src/agent.ts",
    "dry": "bun run src/agent.ts --dry-run",
    "once": "bun run src/agent.ts --once",
    "report": "bun run src/report.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
